import ThemeWrapper from "../ThemeWrapper";

const About = () => {
    return (
        <ThemeWrapper>
            <h1>
                Hello!
            </h1>
        </ThemeWrapper>
    );
};

export default About;